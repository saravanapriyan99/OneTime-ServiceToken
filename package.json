{
  "name": "atomic-token-service",
  "version": "1.0.0",
  "description": "A Redis-backed atomic single-use token service.",
  "main": "token-service.js",
  "scripts": {
    "test": "node test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "redis": "^5.9.0"
  }
}
